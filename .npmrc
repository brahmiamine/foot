# Migration progressive vers pnpm workspace/Turborepo.
# Les applications conservent temporairement leurs lockfiles autonomes et
# leurs installations CI independantes. Ces deux options seront retirees
# lorsque la CI passera a une installation unique a la racine.
shared-workspace-lockfile=false
recursive-install=false
