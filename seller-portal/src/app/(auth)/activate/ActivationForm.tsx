"use client";
import Link from 'next/link';
import { useState } from 'react';
import { Button } from '@/components/ui/Button';
import { FormField, Input } from '@/components/ui/Field';
export function ActivationForm({token}:{token:string}){const[state,setState]=useState<'idle'|'loading'|'done'|'error'>('idle');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('loading');const f=new FormData(e.currentTarget);if(f.get('password')!==f.get('confirm')){setMsg('Les mots de passe ne correspondent pas.');setState('error');return;}const res=await fetch('/api/auth/activate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password:f.get('password')})});if(res.ok)setState('done');else{const body=await res.json().catch(()=>({}));setMsg(body.error||'Activation impossible.');setState('error');}}if(state==='done')return <div><h2>Compte activé</h2><p>Votre espace vendeur est prêt.</p><Link href="/login"><Button>Se connecter</Button></Link></div>;return <form onSubmit={submit}><h2>Activer votre compte vendeur</h2><p>Choisissez votre mot de passe. Ce lien ne peut être utilisé qu’une seule fois.</p><FormField label="Mot de passe" required><Input name="password" type="password" minLength={8} required/></FormField><FormField label="Confirmer" required><Input name="confirm" type="password" minLength={8} required/></FormField>{state==='error'&&<p style={{color:'var(--sp-danger)'}}>{msg}</p>}<Button type="submit" disabled={state==='loading'} style={{width:'100%'}}>{state==='loading'?'Activation…':'Activer le compte'}</Button></form>}
