# Base de données partagée
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=foot

# Sessions SSO : RS256 uniquement. Générer la paire avec
# `pnpm jwt:generate-keypair`; le JWKS public est exposé par identity.
SSO_JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
SSO_JWT_KID=change-me-key-id-1
# Rotation RSA : ancienne clé publiée temporairement dans le JWKS.
SSO_JWT_PRIVATE_KEY_PREVIOUS=
SSO_JWT_KID_PREVIOUS=
SSO_COOKIE_NAME=foot_sso_session
SSO_COOKIE_DOMAIN=
SSO_URL=http://localhost:3004

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SUPERADMIN_EMAIL=
SUPERADMIN_PASSWORD=
SUPERADMIN_NAME=Super Admin

# Service-to-service Identity
SSO_SERVICE_API_KEY=

# Invitations de comptes provisionnés (Player Hub, etc.).
ACCOUNT_INVITATION_TTL_HOURS=48

# Durée maximale d'une preuve MFA récente pour les actions step-up sensibles.
# Bornée côté code entre 60 et 3600 secondes ; défaut 600 (10 minutes).
MFA_STEP_UP_MAX_AGE_SECONDS=600

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@foot-platform.tn
