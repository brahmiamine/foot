# Base partagée avec identity, federation-hub et match-operations
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=foot

# Identity / cookie SSO partagé
SSO_URL=http://localhost:3004
SSO_JWT_SECRET=change-me-to-a-long-random-secret
SSO_COOKIE_NAME=foot_sso_session
SSO_COOKIE_DOMAIN=
SSO_REVOCATION_FAILURE_MODE=closed

# Referee -> Identity : données du profil de compte. Pendant la transition, si
# LES DEUX valeurs sont absentes ProfileService utilise son adapter read-only de
# la base partagée. Avec les deux, il appelle Identity par x-api-key. Une
# configuration partielle est refusée.
IDENTITY_SERVICE_URL=
IDENTITY_SERVICE_API_KEY=

# Destination du bouton « Ouvrir la feuille de match »
MATCH_OPERATIONS_URL=http://localhost:3001

# API interne propriétaire de l'indisponibilité des arbitres. La même valeur
# doit être configurée côté consommateurs (notamment match-operations) dans
# REFEREE_HUB_SERVICE_API_KEY. Sans clé, /api/internal/* échoue en 503.
REFEREE_HUB_SERVICE_API_KEY=change-me-to-a-long-random-secret

# Optionnel : compteur de notifications non lues (best effort)
NOTIFICATION_API_URL=http://localhost:3010
