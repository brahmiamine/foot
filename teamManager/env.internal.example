# Variable à ajouter à .env.local : secret partagé pour authentifier les
# appels internes de matchsheet vers POST/DELETE /api/internal/cards.
# Même valeur que dans matchsheet/env.internal.example.

INTERNAL_API_SECRET=change-me-to-a-long-random-secret
