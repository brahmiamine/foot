import { NextRequest,NextResponse } from "next/server";import { getAdminSession } from "@/lib/adminAuth";import { getDataSource } from "@/lib/db";import { listCompetitionRegistrations } from "@/lib/competitionRegistrations";
export async function GET(request:NextRequest){const session=await getAdminSession(request);if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});const q=new URL(request.url).searchParams;return NextResponse.json(await listCompetitionRegistrations(await getDataSource(),session,{seasonId:q.get("seasonId")??undefined,clubId:q.get("clubId")??undefined,status:q.get("status")??undefined}))}
