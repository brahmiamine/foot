import { NextRequest, NextResponse } from 'next/server'
import { getAdminSession } from '@/lib/adminAuth'
import { getDataSource } from '@/lib/db'
import { StaffContractAuthorizationError, transitionStaffContract } from '@/lib/staffContracts'
import { StaffContractWorkflowError, type StaffContractFederalStatus } from '../../../../../../../../packages/regulatory-shared/src/staffContract'
export const runtime = 'nodejs'
const ACTIONS: Record<string, StaffContractFederalStatus> = { 'start-review': 'UNDER_REVIEW', approve: 'APPROVED', reject: 'REJECTED', cancel: 'CANCELLED' }
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string; action: string }> }) { const session = await getAdminSession(request); if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); const { id, action } = await params; const target = ACTIONS[action]; if (!target) return NextResponse.json({ error: 'Action inconnue' }, { status: 404 }); try { const body = await request.json().catch(() => ({})) as { reason?: string }; return NextResponse.json(await transitionStaffContract(await getDataSource(), session, { userId: session.id, role: session.role, ipAddress: request.headers.get('x-forwarded-for')?.split(',')[0]?.trim() ?? null, userAgent: request.headers.get('user-agent') }, id, target, body.reason)) } catch (error) { if (error instanceof StaffContractAuthorizationError) return NextResponse.json({ error: error.message }, { status: 403 }); if (error instanceof StaffContractWorkflowError) return NextResponse.json({ error: error.message }, { status: 409 }); return NextResponse.json({ error: 'Erreur serveur' }, { status: 500 }) } }
