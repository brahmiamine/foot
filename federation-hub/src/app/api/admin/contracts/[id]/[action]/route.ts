import { NextRequest, NextResponse } from 'next/server'
import { getAdminSession } from '@/lib/adminAuth'
import { getDataSource } from '@/lib/db'
import { PlayerContractAuthorizationError, transitionPlayerContract } from '@/lib/playerContracts'
import { PlayerContractWorkflowError, type PlayerContractFederalStatus } from '../../../../../../../../packages/regulatory-shared/src/playerContract'
export const runtime = 'nodejs'
const ACTIONS: Record<string, PlayerContractFederalStatus> = { 'start-review': 'UNDER_REVIEW', approve: 'APPROVED', reject: 'REJECTED', cancel: 'CANCELLED' }
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string; action: string }> }) { const session = await getAdminSession(request); if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); const { id, action } = await params; const target = ACTIONS[action]; if (!target) return NextResponse.json({ error: 'Action inconnue' }, { status: 404 }); try { const body = await request.json().catch(() => ({})) as { reason?: string }; return NextResponse.json(await transitionPlayerContract(await getDataSource(), session, { userId: session.id, role: session.role, ipAddress: request.headers.get('x-forwarded-for')?.split(',')[0]?.trim() ?? null, userAgent: request.headers.get('user-agent') }, id, target, body.reason)) } catch (error) { if (error instanceof PlayerContractAuthorizationError) return NextResponse.json({ error: error.message }, { status: 403 }); if (error instanceof PlayerContractWorkflowError) return NextResponse.json({ error: error.message }, { status: 409 }); console.error('Error applying contract decision:', error); return NextResponse.json({ error: 'Erreur serveur' }, { status: 500 }) } }
